sudo: required 
language: generic

services:
  - docker

before_install:
  - docker build -t 510ravikothari/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true 510ravikothari/docker-react npm run test